<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mabatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 
	SQL File Name : 
	Description : 설명을 기술
	author : USER
	since : 2023.01.01
 -->
 
 <!-- 
 <mapper namespace="CommonRepository">
 	<sql id="pageHeader">
 		SELECT * 
 		FROM (
 			SELECT ROWNUM AS PAGE_ROWNUM,
 				   DATA.*
 			FROM (
 	</sql>
 	
 	<sql id="pageFooter">
 			) DATA
 		)
 		WHERE PAGE_ROWNUM BETWEEN ((#{page} -1) * #{limit}) AND (#{page} * #{limit})
 	</sql>
 </mapper>
 -->